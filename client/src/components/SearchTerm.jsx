import {Col, Nav, Collapse, Image} from "react-bootstrap";
import { useState } from 'react'

const SearchTerm = props => {

    return (
        <>
            <p>Search Term Clustering</p>
        </>
    )
}

export default SearchTerm